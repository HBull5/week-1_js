/**
 * Declare all global variables
 */
var x;
var y = 1;
var arr = [];
var rememberMe;
var obj = {};

/**
 * define functions
 */
function foo() {
	// do stuff
}

function bar(param) {
	// do stuff w/ param
}

/**
 * application logic lives, calling functions
 * function calls, event listeners, if stmts, loops
 */

console.log(obj);

foo();

//! paradigms: OLN, OOP -> unnecessary, procedural, functional
